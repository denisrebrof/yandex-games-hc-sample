using UnityEngine;

namespace Balance.presentation
{
    public class BalanceUnityEvent : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
